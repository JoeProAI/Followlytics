// Next.js instrumentation hook - not needed for PostHog client-only setup
// Keeping file for future server-side instrumentation
export async function register() {
  // PostHog runs client-side via instrumentation-client.ts
}
